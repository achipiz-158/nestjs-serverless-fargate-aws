export enum StateCancellation {
  NC = 'no cancellable',
  REVIEW = 'in review',
  REJECTED = 'rejected',
  CET = 'cancellation expired term',
  CBH = 'canceled by host',
  CR = ' cancellation with refund',
  ABA = 'accepted by the administrator',
}
